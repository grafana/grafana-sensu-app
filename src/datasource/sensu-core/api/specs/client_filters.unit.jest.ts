import { getClientsWithFilter } from '../client_filters';

describe('Sensu Core Client Filters', () => {
  describe('when calling getClientsWithFilter()', () => {
    it.skip('TODO', () => {});
  });
});
